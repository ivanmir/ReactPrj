{
  "name": "project",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "@sap/approuter": "14.3.2",
    "@sap/html5-repo-mock": "2.1.9"
  },
  "scripts": {
    "build-app": "cd app && if not exist ${workspaceFolder}/ReactPrj/app/node_modules/react-scripts ( if not exist ${workspaceFolder}/ReactPrj/node_modules/react-scripts ( npm i ) ) && npm run build"
  }
}
